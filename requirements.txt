# Pinned so an unattended daily job cannot be broken by an upstream release.
# Review roughly yearly; see DOCUMENTATION.md "Long-term durability risks".
requests>=2.32,<3
beautifulsoup4>=4.12,<5
pandas>=2.2,<3
openpyxl>=3.1,<4      # only needed to read the legacy .xlsx archive
