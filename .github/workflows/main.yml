name: Run Finnpanel Scraper

on:
  schedule:
    - cron: '0 8,20 * * *'  # Ajaa klo 8:00 ja 20:00 UTC päivittäin
  workflow_dispatch:  # Sallii manuaalisen käynnistyksen

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3  # Päivitetty v2:sta v3:een
    - name: Set up Python
      uses: actions/setup-python@v4  # Päivitetty v2:sta v4:ään
      with:
        python-version: '3.x'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Run scraper
      env:
        AIRTABLE_API_KEY: ${{ secrets.AIRTABLE_API_KEY }}
        AIRTABLE_BASE_ID: ${{ secrets.AIRTABLE_BASE_ID }}
        AIRTABLE_TABLE_NAME: ${{ secrets.AIRTABLE_TABLE_NAME }}
      run: python FP_scraper.py
    - name: Commit and push if changes
      run: |
        git config --global user.name 'GitHub Action'
        git config --global user.email 'action@github.com'
        git add -A
        git diff --quiet && git diff --staged --quiet || (git commit
